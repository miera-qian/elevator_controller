# 电梯调度算法 - 快速上手指南

> 本指南将带您从零开始，10分钟内完成算法测试

## 📋 目录

1. [环境准备](#环境准备)
2. [项目安装](#项目安装)
3. [运行测试](#运行测试)
4. [查看结果](#查看结果)
5. [常见问题](#常见问题)

---

## 环境准备

### 第一步：确认 Python 版本

```bash
python3 --version
```

**要求**：Python 3.12 或更高版本

如果版本不符合要求，请访问 [python.org](https://www.python.org/downloads/) 下载最新版本。

### 第二步：安装 uv

uv 是一个快速的 Python 包管理工具，用于管理项目依赖。

#### macOS / Linux

```bash
# 方法1：使用 Homebrew（推荐 macOS 用户）
brew install uv

# 方法2：使用安装脚本（通用）
curl -LsSf https://astral.sh/uv/install.sh | sh
```

#### Windows

```powershell
# 使用 PowerShell
irm https://astral.sh/uv/install.ps1 | iex
```

#### 验证安装

```bash
uv --version
```

看到版本号（如 `uv 0.4.0`）即表示安装成功。

---

## 项目安装

### 第一步：进入项目目录

```bash
cd /path/to/elevator_homework
```

### 第二步：安装项目依赖

```bash
uv sync
```

这个命令会：
- 创建虚拟环境（`.venv/`）
- 安装所有必需的依赖包（`elevator-py` 等）
- 锁定依赖版本（`uv.lock`）

**预计时间**：30秒 - 2分钟（取决于网络速度）

### 第三步：验证安装

```bash
uv run python -c "import elevator; print('✓ 安装成功')"
```

看到 `✓ 安装成功` 即可进入下一步。

---

## 运行测试

### 🎯 方式一：自动化测试（强烈推荐）⭐⭐⭐

**特点**：全自动，无需任何手动操作

#### 快速测试（5-10分钟）

测试 3 个小型场景，快速验证算法：

```bash
uv run python tests/auto_test.py \
    --scenarios small_morning_rush,small_evening_rush,small_burst_traffic
```

#### 完整测试（20-40分钟）

测试所有 10 个场景，全面评估算法性能：

```bash
uv run python tests/auto_test.py
```

#### 单算法测试

只测试优化 SCAN 算法：

```bash
uv run python tests/auto_test.py --algorithms OptimizedScanAlgorithm
```

只测试强化学习算法：

```bash
uv run python tests/auto_test.py --algorithms RLDQNAlgorithm
```

#### 测试过程示例

```
======================================================================
Automated Testing
======================================================================
Algorithms: OptimizedScanAlgorithm, RLDQNAlgorithm
Scenarios: 3
Total tests: 6
======================================================================

[1/6] OptimizedScanAlgorithm on small_morning_rush

  Scenario: small_morning_rush
    小型建筑上班高峰 - 6层楼，2部电梯
    6 floors, 2 elevators, 160 passengers
Starting simulator server on port 8000...
  ✓ Server is ready
    Running OptimizedScanAlgorithm... ✓ Avg=122.5, P95=194.0
  Stopping server...
  ✓ Server stopped

[2/6] OptimizedScanAlgorithm on small_evening_rush
...
```

**测试完成后会显示**：

```
======================================================================
Testing Complete!
Total time: 15.2 minutes
Success rate: 100%
======================================================================

Results saved to: test_results/auto_test_20251003_203355.json
Report saved to: docs/auto_test_report_20251003_203355.md
```

---

### 🔧 方式二：手动测试（适合深度调试）

如果需要手动控制测试过程，可以使用这种方式。

#### 第一步：启动模拟器服务器

打开**第一个终端窗口**：

```bash
uv run -m elevator server
```

服务器启动成功后会显示：

```
INFO:     Started server process
INFO:     Uvicorn running on http://127.0.0.1:8000
```

**保持这个终端窗口运行，不要关闭！**

#### 第二步：运行批量测试

打开**第二个终端窗口**：

```bash
uv run python tests/run_tests.py
```

#### 第三步：按照提示操作

程序会逐个展示测试场景，您需要：

1. **查看场景信息**（楼层数、电梯数、乘客数等）
2. **按回车运行测试**
3. **等待测试完成**（10-60秒）
4. **复制粘贴性能指标**（从第一个终端窗口）
5. **继续下一个场景**

#### 第四步：生成报告

所有场景测试完成后，程序会自动生成测试报告。

---

## 查看结果

### 测试报告位置

自动化测试完成后会生成两个文件：

1. **JSON 原始数据**
   ```
   test_results/auto_test_20251003_203355.json
   ```

2. **Markdown 可读报告**
   ```
   docs/auto_test_report_20251003_203355.md
   ```

### 打开查看报告

```bash
# macOS
open docs/auto_test_report_*.md

# Linux
xdg-open docs/auto_test_report_*.md

# Windows
start docs\auto_test_report_*.md

# 或使用任意文本编辑器
vim docs/auto_test_report_20251003_203355.md
```

### 报告内容示例

```markdown
# 自动化测试报告
生成时间: 2025-10-03 20:33:55
---

## 测试概览

**测试算法**: OptimizedScanAlgorithm, RLDQNAlgorithm
**测试场景数**: 3
**测试方法**: 每个测试都在独立的服务器实例中运行，确保完全隔离

## 详细测试结果

| 算法 | 场景 | 规模 | 平均等待 | P95等待 | 平均系统时间 | 完成率 | 状态 |
|------|------|------|----------|---------|-------------|--------|----- |
| OptimizedScanAlgorithm | small_morning_rush | 6层/2梯/160人 | 122.5 | 194.0 | 122.5 | 100.0% | ✅ |
| RLDQNAlgorithm | small_morning_rush | 6层/2梯/160人 | 104.0 | 190.0 | 120.0 | 100.0% | ✅ |

## 算法性能统计

| 算法 | 成功率 | 平均等待时间 (均值) | P95等待时间 (均值) | 完成率 (均值) |
|------|--------|---------------------|--------------------|--------------|
| OptimizedScanAlgorithm | 100% | 122.47 | 194.00 | 100.0% |
| RLDQNAlgorithm | 100% | 104.34 | 190.50 | 100.0% |
```

### 理解性能指标

| 指标 | 含义 | 越低越好 |
|------|------|---------|
| **平均等待** | 乘客从按电梯到进入电梯的平均时间 | ✅ |
| **P95等待** | 95%的乘客等待时间不超过这个值 | ✅ |
| **平均系统时间** | 从按电梯到到达目的地的总时间 | ✅ |
| **完成率** | 成功服务的乘客比例 | 越高越好 |

**好的算法表现**：
- 平均等待时间：< 120 ticks
- P95 等待时间：< 200 ticks
- 完成率：100%

---

## 常见问题

### ❓ 安装 uv 时提示权限错误

**问题**：
```
Permission denied
```

**解决**：

```bash
# macOS/Linux
sudo curl -LsSf https://astral.sh/uv/install.sh | sh

# 或者修改安装目录
export UV_HOME="$HOME/.local"
curl -LsSf https://astral.sh/uv/install.sh | sh
```

---

### ❓ uv sync 时下载速度很慢

**问题**：依赖包下载缓慢

**解决**：使用国内镜像

```bash
# 设置 pip 镜像（临时）
export UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple

# 然后再运行
uv sync
```

---

### ❓ 运行测试时提示 "Port 8000 is in use"

**问题**：
```
⚠ Port 8000 is in use, waiting for it to be freed...
```

**原因**：端口 8000 被其他程序占用

**解决**：

```bash
# 查找占用端口的进程
lsof -i :8000

# 杀死进程（将 <PID> 替换为实际进程ID）
kill -9 <PID>

# 或者一键清理
lsof -ti:8000 | xargs kill -9
```

---

### ❓ 测试时显示 "Server failed to start"

**问题**：服务器启动失败

**原因**：可能是环境问题或依赖未正确安装

**解决步骤**：

1. **重新安装依赖**
   ```bash
   uv sync --reinstall
   ```

2. **验证 elevator-py 安装**
   ```bash
   uv run python -c "import elevator; print(elevator.__version__)"
   ```

3. **手动测试服务器启动**
   ```bash
   uv run -m elevator server
   ```

   如果手动启动成功，说明自动化脚本有问题；如果失败，说明环境有问题。

---

### ❓ 测试超时 "Timeout (300s exceeded)"

**问题**：某个场景测试超过 5 分钟

**原因**：场景规模太大或系统负载过高

**解决**：

1. **只测试小型场景**
   ```bash
   uv run python tests/auto_test.py --scenarios small_morning_rush
   ```

2. **关闭其他占用资源的程序**

3. **增加超时时间**（编辑 `tests/auto_test.py`）
   ```python
   def run_algorithm(self, algorithm_name: str, scenario_name: str,
                    timeout: int = 600):  # 改为 600 秒（10分钟）
   ```

---

### ❓ 手动测试时找不到性能指标输出

**问题**：第一个终端窗口没有看到性能数据

**原因**：测试可能失败或输出被隐藏

**解决**：

1. **检查算法是否正常运行**
   ```bash
   # 在第二个终端窗口
   uv run python -c "
   from algo import OptimizedScanAlgorithm
   algorithm = OptimizedScanAlgorithm(enable_logging=True)
   algorithm.start()
   "
   ```

2. **查看服务器日志**

   在第一个终端窗口应该能看到大量日志输出，最后会有类似这样的性能指标：
   ```
   {
       'average_wait_time': 122.47,
       'p95_wait_time': 194.0,
       ...
   }
   ```

---

### ❓ 两个算法哪个更好？

**OptimizedScanAlgorithm（优化 SCAN 算法）**
- ✅ 性能稳定可靠
- ✅ 无需训练，立即可用
- ✅ 适合各种场景
- ⚠️ 性能可能不是最优

**RLDQNAlgorithm（强化学习算法）**
- ✅ 可以自适应学习
- ✅ 长期运行后性能更优
- ⚠️ 需要训练期
- ⚠️ 未训练时表现可能较差

**推荐**：
- 生产环境：使用 **OptimizedScanAlgorithm**
- 研究实验：尝试 **RLDQNAlgorithm** 并训练

---

### ❓ 如何添加自己的算法？

查看详细指南：[算法扩展指南](algo/README.md)

**简要步骤**：

1. 在 `algo/` 目录创建新文件 `my_algorithm.py`
2. 继承 `BaseAlgorithm` 类
3. 实现必需的方法（`on_passenger_call` 等）
4. 在 `algo/__init__.py` 中注册算法
5. 运行测试验证

```python
# algo/my_algorithm.py
from algo.base_algorithm import BaseAlgorithm

class MyAlgorithm(BaseAlgorithm):
    def on_passenger_call(self, ...):
        # 实现你的调度逻辑
        pass
```

```python
# algo/__init__.py
from .my_algorithm import MyAlgorithm

__all__ = [
    'BaseAlgorithm',
    'OptimizedScanAlgorithm',
    'RLDQNAlgorithm',
    'MyAlgorithm',  # 添加这一行
]
```

测试新算法：
```bash
uv run python tests/auto_test.py --algorithms MyAlgorithm
```

---

## 🚀 下一步

### 深入学习

- 📖 [完整 README](README.md) - 项目详细介绍
- 📘 [算法设计文档](docs/algorithm.md) - 了解算法原理
- 📙 [自动化测试完整指南](docs/automated_testing_guide.md) - 深入理解测试系统

### 高级用法

**对比两个算法的性能**：
```bash
uv run python tests/auto_test.py \
    --algorithms OptimizedScanAlgorithm,RLDQNAlgorithm \
    --scenarios small_morning_rush,medium_inter_floor
```

**测试极限场景**：
```bash
uv run python tests/auto_test.py \
    --scenarios large_mixed,xlarge_stress_test
```

**生成自定义测试场景**：
```bash
# 编辑 tests/generate_test_data.py 添加场景定义
uv run python tests/generate_test_data.py
```

### 开发工作流

1. **修改算法** → 编辑 `algo/` 目录下的文件
2. **快速验证** → `uv run python tests/auto_test.py --scenarios small_morning_rush`
3. **完整测试** → `uv run python tests/auto_test.py`
4. **查看报告** → 打开生成的 Markdown 文件
5. **提交代码** → `git commit` & `git push`

---

## 💡 提示与技巧

### 加速测试

```bash
# 只测试小型场景（最快，适合开发迭代）
uv run python tests/auto_test.py \
    --scenarios small_morning_rush,small_evening_rush,small_burst_traffic

# 只测试单个算法（中等速度）
uv run python tests/auto_test.py --algorithms OptimizedScanAlgorithm

# 完整测试（最慢，提交前必做）
uv run python tests/auto_test.py
```

### 保持环境整洁

```bash
# 定期清理测试结果
rm test_results/auto_test_*.json
rm docs/auto_test_report_*.md

# 重新安装依赖（遇到奇怪问题时）
rm -rf .venv/
uv sync
```

### 性能基准参考

| 场景规模 | 优秀 | 良好 | 可接受 | 需改进 |
|---------|------|------|--------|--------|
| 小型（6层） | < 80 | 80-120 | 120-180 | > 180 |
| 中型（10层） | < 100 | 100-150 | 150-220 | > 220 |
| 大型（15-20层） | < 140 | 140-200 | 200-280 | > 280 |
| 超大（25层+） | < 180 | 180-250 | 250-350 | > 350 |

*单位：ticks（平均等待时间）*

---

## 🆘 获取帮助

### 文档资源

- [项目 README](README.md)
- [算法扩展指南](algo/README.md)
- [测试指南](docs/testing_guide.md)
- [自动化测试详细文档](docs/automated_testing_guide.md)

### 外部资源

- [Elevator 项目官方文档](https://zgca-forge.github.io/Elevator)
- [uv 官方文档](https://docs.astral.sh/uv/)

### 问题反馈

如遇到本指南未覆盖的问题，请：
1. 查看详细文档（见上方链接）
2. 检查 GitHub Issues
3. 提交新的 Issue

---

## 🖼️ 可选：Web 可视化界面（仅供演示）

**重要提示**: WebUI 使用简化的 Mock 模拟引擎，**不运行真实算法**，性能数据仅供参考。

### 用途

- ✅ **教学演示**: 直观展示电梯调度过程
- ✅ **UI 开发**: 测试可视化效果
- ⚠️ **不适用于**: 算法性能评估、算法对比研究

### 启动方式

```bash
uv run python -m webui.app
```

然后在浏览器中访问 http://localhost:8080

### 使用步骤

1. 页面自动加载默认场景的电梯布局
2. （可选）选择其他测试场景 → 可视化立即更新
3. （可选）调整模拟速度（0.1x - 5x）
4. 点击"开始模拟"按钮
5. 观察电梯实时移动和乘客上下
6. 查看实时统计信息

### 特性

- 实时可视化电梯位置和乘客
- 平滑动画效果
- 颜色编码状态（灰=空闲，绿=上行，橙=下行）
- 模拟完成后电梯自动归位到1层

### ⚠️ 重要限制

**WebUI 的局限性**:
- 使用简化的最近电梯分配策略
- 不运行真实的 OptimizedScan/RL/Hybrid 算法
- 性能指标仅供参考，不能用于算法评估

**如需运行真实算法**: 请使用**方式一：自动化测试**或 CLI 工具（`uv run python main.py run`）

---

**祝测试顺利！🎉**

*最后更新：2025-10-04*
